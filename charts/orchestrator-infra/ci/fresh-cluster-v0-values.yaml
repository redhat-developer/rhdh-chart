# Fresh-cluster OLM v0 install: do not pre-create Knative CRDs before chart install.
olmVersion: v0

serverlessLogicOperator:
  enabled: true
  subscription:
    namespace: operators
    spec:
      sourceNamespace: olm

serverlessOperator:
  subscription:
    namespace: operators
    spec:
      sourceNamespace: olm
