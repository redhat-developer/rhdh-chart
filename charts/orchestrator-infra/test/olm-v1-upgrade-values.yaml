# OLM v1 upgrade target values. Used by the v0-to-v1 upgrade path test.
olmVersion: v1

serverlessLogicOperator:
  enabled: true
  subscription:
    namespace: operators
    spec:
      sourceNamespace: olm

serverlessOperator:
  subscription:
    namespace: operators
    spec:
      sourceNamespace: olm
