# Workaround for kind cluster in CI which has no Routes and no PVCs
route:
  enabled: false
upstream:
  postgresql:
    primary:
      persistence:
        enabled: false

test:
  image:
    registry: quay.io
    repository: rhdh-community/curl
    tag: latest
